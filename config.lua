-- Daseeki Chat — config.lua
-- Wave 2: reconciler/sync agent. The authoritative config model + SavedVariables
-- shape + capture-back diffing (design doc D3). Placeholder: registers nothing.

local ADDON, ns = ...
ns.Config = ns.Config or {}
